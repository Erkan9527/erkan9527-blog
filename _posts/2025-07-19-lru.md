---
title: LRU缓存实现
author: Erkan9527
date: 2025-07-19
category: 技术
layout: post
---

## 题目

[LRU缓存](https://leetcode.cn/problems/lru-cache/)

```
class LRUCache {
 private:
 int capacity;
 list<pair<int,int>>cache;
 unordered_map<int,list<pair<int,int>>>mp;

public:
    LRUCache(int capacity):capacity(capacity) {
        
    }
    
    int get(int key) {
        

        if(mp.find(key)==mp.end()){
return -1;
        }
else{
auto it=mp[key];
int value=it->second;
cache.erase(it);
cache.push_front({key,value});
mp[key]=cache.begin();
return value;

}

    }
    
    void put(int key, int value) {
        // 如果键已存在，更新值并移动到链表头部
        if (mp.find(key) != mp.end()) {
            auto it = mp[key];
            cache.erase(it); // 从链表中删除旧位置
        } else {
            // 如果缓存已满，移除链表尾部的最久未使用的键值对
            if (cache.size() == capacity) {
                auto last = cache.back();
                mp.erase(last.first); // 从哈希表中删除对应的键
                cache.pop_back(); // 从链表中删除尾部元素
            }
        }
        // 插入新的键值对到链表头部
        cache.push_front({key, value});
        mp[key] = cache.begin(); // 更新哈希表中的迭代器
    
    }
};

/**
 * Your LRUCache object will be instantiated and called as such:
 * LRUCache* obj = new LRUCache(capacity);
 * int param_1 = obj->get(key);
 * obj->put(key,value);
 */

```